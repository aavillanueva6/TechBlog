{{#each posts as |post|}}
<div class="card mt-4">
  <header class="card-header is-justify-content-space-between">
    <div>
      <p class="card-header-title">
        {{post.title}}
      </p>
    </div>
  </header>
</div>
{{/each}}